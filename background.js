chrome.commands.onCommand.addListener(function(command) {
    if (command == "miComando") {
      // Realiza la acción deseada cuando se oprime la tecla correspondiente
    
      let uwu = document.getElementsByClassName("button button-green-dimmed mx-auto h-9 px-3 text-2xs")
      for(u of uwu){
        u.click()
        }
      console.log("uwu")
    }
  });
  